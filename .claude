# 项目要求规范
## 说明用的索引文件
* 除项目入口目录使用`README.md`外，其他项目内的目录都需要创建一个markdown文件，以说明当前目录作用；
* "README.md"为整个项目的说明，包含功能说明，重要命令、运行方式等；
* 命名方式为当前目录名，例如目录为"xxx"则说明文件为"xxx.md"；

## 时间管理
项目中的时间存储使用展示都使用UTC时间，在数据库存储过程中最好都带上数据库UTC时间的标志。